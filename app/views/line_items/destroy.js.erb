$('#cart_price').html('<%= number_to_currency @cart.total_price %>');
$('#<%= @item.id %>').remove();
$(document).ready(function() {
  $('#cart_size').html('<%= cart_size %>');
  $('#cart').html('<%= cart_size %>');
  toastr.success('<%= t "remove_from_cart" %>');
});
